[defaults]
inventory         = inventory.ini
remote_user       = debian
host_key_checking = False
strategy          = linear
order             = inventory

[privilege_escalation]
become            = True
become_ask_pass   = False
become_method     = sudo
become_user       = root